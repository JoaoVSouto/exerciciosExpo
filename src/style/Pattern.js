import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    ex: {
        paddingHorizontal: 15,
        marginHorizontal: 6,
        marginVertical: 70,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: '#222',
        fontSize: 24,
        fontWeight: 'bold'
    }
});
